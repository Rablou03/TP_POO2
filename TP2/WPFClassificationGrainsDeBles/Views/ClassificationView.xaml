<UserControl x:Class="WPFClassificationGrainsDeBles.Views.ClassificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" Width="600">

            <TextBlock Text="Classification"
                       FontSize="20"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0 0 0 25"/>

            <GroupBox Header="Exécution" Margin="0 0 0 20">
                <StackPanel Margin="10">
                    <Button Content="Lancer la classification"
                            Command="{Binding ClassifyCommand}"
                            Height="40"
                            FontWeight="Bold"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Résultats">
                <DataGrid ItemsSource="{Binding Resultats}"
                          AutoGenerateColumns="True"
                          Margin="10"
                          Height="300"/>
            </GroupBox>

        </StackPanel>
    </Grid>
</UserControl>
